[package]
name = "asl-store"
description = "L'entrepôt durable : redb, des tables, et rien qui décide."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
asl-id.workspace = true
asl-registre.workspace = true
redb.workspace = true

# `asl-auth` POUR LES ESSAIS SEULEMENT, et c'est la bonne place : l'entrepôt
# range l'EMPREINTE d'un code d'enrôlement, qu'il reçoit toute faite ; il ne sait
# pas la calculer et n'a aucune raison de savoir. Les essais, eux, doivent bien
# en fabriquer une.
[dev-dependencies]
asl-auth.workspace = true

[lints]
workspace = true
